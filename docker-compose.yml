services:
  backend:
    image: ghcr.io/${GITHUB_REPOSITORY_OWNER}/ikkiattor-backend:latest
    container_name: ikkiattor-backend
    restart: always
    volumes:
      - ikkiattor-db:/app/data
    environment:
      - DATABASE_URL=file:/app/data/dev.db
      - JWT_SECRET=${JWT_SECRET:-ikkiattor-super-secret-key}
      - PORT=3001
    command: sh -c "mkdir -p /app/data && npx prisma migrate deploy && npm run seed && npm start"
    networks:
      - app-network

  frontend:
    image: ghcr.io/${GITHUB_REPOSITORY_OWNER}/ikkiattor-frontend:latest
    container_name: ikkiattor-frontend
    restart: always
    ports:
      - "3000:80"
    depends_on:
      - backend
    networks:
      - app-network

networks:
  app-network:
    driver: bridge

volumes:
  ikkiattor-db:
